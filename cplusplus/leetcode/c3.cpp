
#include<iostream>
#include<vector>

struct Interval {
    int start;
    int end;
    Interval() : start(0), end(0) {}
    Interval(int s, int e) : start(s), end(e) {}
};

vector<Interval> merge(vector<Interval>& intervals) {
		
}

int main() {
	Interval a(1,3);	
	Interval b(2,6);	
	Interval b(8,10);	
	Interval b(8,10);	

	vector<Interval> nums= {a,b,c,d};

	merge(nums);
}


